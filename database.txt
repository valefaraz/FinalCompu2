name: scc
user: valentin
pass: valentin

localhost:3306

comandos sql

create table mediciones (id int unsigned not null primary key auto_increment,id_sensor int unsigned not null,foreign key (id_sensor) references sensores(id), valor int not null,fecha datetime);

**consultar el ultimo registro con id_sensor=1**
select * from mediciones where id_sensor='1' order by id desc limit 1;

select mediciones.id,id_sensor,valor,fecha,s.tipo from mediciones join sensores as s on mediciones.id_sensor=s.id;

